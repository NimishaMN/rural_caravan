<div class="content">
  <section id="content">
    <section class="main padder">
      <div class="row">
        <div class="col-lg-4">
          <h2><i class="icon-user text"></i></i>Incomes</h2>
        </div>
        <div class="col-lg-8 m-t-large" align="right">
          <div class="btn-group">
            <button  onclick="location.href= '<%= new_income_path %>'"; class="btn btn-success btn-lg"; disabled = "current_user.subscription_end_date.present?" > <i class="icon-plus"> </i>Add New Transaction</button>
            </button>
         </div>
        </div>
      </div>
 <div class="panel-body">
                   
       <div class="row">
        <div class="col-lg-12"><p></p>
          <section class="panel">
           <div class="panel-body">
              <div class="table-responsive">
                <table id="incomes" class="table table-striped m-b-none text-small">
                  <thead>
                    <tr>
                      <th>Vendor Name</th>
                      <th>Amount</th>
                      <th>Date</th>
                      <th></th>
                      <th>Actions</th>
                      <th></th>
                      
                    </tr>
                  </thead>
                  <tbody>
                    <% if @incomes.blank? %>
                        <%= render :partial=>"/shared/no_data_found"%>
                      <% else %>

    <% @incomes.each do |income| %>
      <tr>
        <td><%= income.customer.name%></td>
        <td><%= income.amount%></td>
        <td><%= income.income_date%></td>
        <td><%= link_to 'Show', income %></td>
        <td><%= link_to 'Edit', edit_income_path(income) %></td>
        <td><%= link_to 'Delete', income, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  <% end %>
                  </tbody>
                </table>
              </div>
              <div class="text-center">
              
              </div>              
            </div>  
          </section>
        </div>
      </div>
   </section>
   </section>
  </div>
