<%= form_with(model: work_detail, local: true) do |f| %>
  <% if work_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(work_detail.errors.count, "error") %> prohibited this work_detail from being saved:</h2>

      <ul>
      <% work_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :unit_of_measure %><br>
    <%= f.text_field :unit_of_measure %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
