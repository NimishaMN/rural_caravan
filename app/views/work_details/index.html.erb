<p id="notice"><%= notice %></p>

<h1>Work Details</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <th>Name</th>
      <th>Unit of Measure</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
    <% @work_details.each do |work_detail| %>
      <tr>
        <td> <%= work_detail.name %></td>
        <td> <%= work_detail.unit_of_measure %></td>
        <td><%= link_to 'Show', work_detail %></td>
        <td><%= link_to 'Edit', edit_work_detail_path(work_detail) %></td>
        <td><%= link_to 'Destroy', work_detail, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Work Detail', new_work_detail_path %>
